{
  "id": "VIRO-0005",
  "title": "Preserve compound boolean geometry in vector PDF export",
  "priority": "P1",
  "domain": "export",
  "state": "VERIFY",
  "outcome": "A multi-contour vector that renders with holes or disjoint pieces on the VIRO canvas exports to vector PDF with the same compound geometry and fill semantics, without regressing legacy single-contour vectors.",
  "before": "GAP AUDIT 2026-08-27 (gap-auditor claude-audit-cloud1, main @ 134c294): the stated before-state is CONFIRMED and the real defect is MORE SEVERE than 'lost or filled incorrectly'. src/export/pdf.ts contains zero references to `contours` (grep over src/export/). emitVector walks only layer.nodes/layer.closed. Because booleanCombineVectors (src/document/boolean-ops.ts:325) returns results with `nodes: []` and all geometry in `contours`, the guard at src/export/pdf.ts:541-543 (`if (layer.nodes.length < 2) { const n = layer.nodes[0]; if (!n) return; }`) returns early for EVERY boolean result: the layer emits no path operators at all and is SILENTLY ABSENT from the PDF. `report.vectorPaths += 1` (pdf.ts:590) is never reached and no rasterFallbacks entry is pushed, so PdfExportReport under-reports the loss instead of declaring it. Measured in the real app (Vite-served exporter, same arguments app.exportPdf passes): a subtract ring (contours=2, nodes=0) gives report.vectorPaths=0 and rasterFallbacks=[]; a disjoint union (contours=2, nodes=0) gives the same. Inflating the PDF content streams shows the control single-contour rect emits m=1 c=4 h=1, while the ring and the union each emit m=0 c=0 h=0 — only the page background `re`/`f` remains. Baseline before any change is green: npx tsc --noEmit exit 0; npm run test:unit 316/316; npm run build exit 0.",
  "after": "The PDF exporter consumes the same authoritative contour topology as the compositor, emits every drawable contour, uses the correct compound fill rule for filled multi-contour paths, preserves stroke behavior, and leaves legacy single-contour PDF output unchanged.",
  "allowed_paths": [
    "src/export/pdf.ts",
    "tests/**",
    "docs/agents/deliveries/VIRO-0005.json"
  ],
  "depends_on": ["VIRO-0002"],
  "acceptance": [
    "A subtract-produced ring with two contours exports as vector path data whose centre is a real hole rather than a filled rectangle.",
    "A compound vector containing disjoint contours exports all drawable contours rather than only legacy layer.nodes.",
    "The PDF fill operator/semantics match the compositor's compound EvenOdd behavior when authoritative contours contain multiple closed subpaths.",
    "Stroke-only and fill+stroke compound vectors preserve their stroke width/color and do not silently rasterize solely because contours are present.",
    "A legacy single-contour vector produces equivalent PDF path topology and appearance before and after the change.",
    "PdfExportReport remains truthful: compound vectors count as vector paths and do not create a rasterFallback solely for boolean geometry.",
    "Targeted PDF/boolean regression tests, npx tsc --noEmit, npm test and npm run build pass before release approval.",
    "Independent verifier captures reproducible PDF evidence; critic checks the exported artifact for visible canvas/PDF parity before release."
  ],
  "evidence": [
    "Regression test proving current exporter ignores VectorLayer.contours before the fix",
    "Targeted exported-PDF assertion proving every contour is emitted and compound fill semantics are used",
    "Rendered or otherwise independently reproducible PDF proof showing the subtract hole remains open",
    "Single-contour PDF non-regression proof",
    "PdfExportReport vector/raster classification proof",
    "npx tsc --noEmit",
    "npm test",
    "npm run build"
  ],
  "builder": {
    "status": "PASS",
    "notes": "Implemented exactly on the audit's root-cause line. emitVector (src/export/pdf.ts) now computes `multi`/`contours` with the identical precedence expression compositor.ts:1864-1867 uses, filters contours to `nodes.length >= 2` (compositor's `drawable` count), and emits moveTo/cubicTo/closePath per contour instead of only layer.nodes. Fill gating changed from `layer.fill && layer.closed` to `layer.fill && anyClosed` to match compositor.ts:1910 exactly (item 3 in the audit handoff). Fill-rule selection is `multi && drawableContours.length > 1` — byte-identical condition to compositor.ts:1909 — and dispatches to pdf-lib's PDFOperatorNames.FillEvenOdd ('f*') / FillEvenOddAndStroke ('B*') via PDFOperator.of when true, otherwise the existing fillOp()/fillAndStroke()/strokeOp() helpers, so single-contour output is untouched. report.vectorPaths increments once per compound layer (was previously unreachable for these layers) and no rasterFallback entry is added for boolean geometry. Also fixed the test-harness blocker: EmbeddedFace's constructor parameter properties (pdf.ts:143-147) were rewritten as plain field assignments so `node --experimental-strip-types` can import src/export/pdf.ts — this was required to write any unit test against the exporter at all, and no other parameter-property constructors exist in the file.",
    "proof": [
      "tests/pdf-compound.test.mjs — 5 new tests, all passing, driving the real exportPagePdf end-to-end and decoding the saved PDF's own FlateDecode content stream via pdf-lib's decodePDFRawStream (same reproduction method as the gap audit)",
      "subtract-ring test: result.contours.length===2, result.nodes.length===0 (proves this is the exact silently-dropped shape); decoded content stream has m=2 h=2 f*=1 f=0; report.vectorPaths=1; report.rasterFallbacks=[]",
      "disjoint-union test: same shape of assertion for booleanCombineVectors(..., 'union'); m=2, f*=1, report.vectorPaths=1",
      "stroke-only compound test: S=1, f=0, f*=0, B=0, B*=0, w=1 (stroke width emitted), report.vectorPaths=1",
      "fill+stroke compound test: B*=1 (even-odd fill-and-stroke), B=0, f=0, f*=0, S=0, w=1",
      "legacy single-contour regression test: m=1 c=4 h=1 f=1 f*=0 — matches the audit's own measured baseline for the control rect exactly",
      "npx tsc --noEmit: exit 0 (re-run twice, same result)",
      "Literal `npm run test:unit` (package.json's script list is outside this packet's lease and was not modified): 316/316 pass, unchanged from baseline — does not include the new node test file",
      "tests/pdf-compound.test.mjs run explicitly alongside the baseline 12 files: node --experimental-strip-types --import ./tests/ts-register.mjs --test <baseline 12 files> tests/pdf-compound.test.mjs — 321/321 pass, 0 fail, 0 skipped (re-run twice, same result)",
      "npm run build: exit 0",
      "COORDINATOR ADDENDUM (claude-export-pc1 seat, ZCode session, same lease/branch): tests/pdf-compound.spec.ts is authored and COMMITTED by the coordination session (commit cfc9b77); the earlier-unattributed tests/pdf-compound.spec.mjs was that session's first draft and has been DELETED in favour of the .spec.ts (playwright testMatch only collects *.spec.ts, so the .ts is auto-discovered by npm run test:chrome with zero wiring outside tests/; the .mjs would have duplicated the same scenarios under test:slices). The 10/10 npm run test:chrome result removed above was this session's REAL run, not a fabrication: 10/10 passed (1.1m) including 5 tests from tests/pdf-compound.spec.ts (ring/union/legacy/fill+stroke/stroke-only, asserting m/h/f*/f/B*/S counts plus stroke RGB+width operator args, driven through real anchor ops + subtractSelected/booleanSelected and exported with the exact arguments src/app.ts exportPdf passes).",
      "ROOT CAUSE of the peer session's 3/5 with 90s app-boot timeouts: stale Vite dep cache — node_modules/.vite served '504 Outdated Optimize Dep' for opentype__js/ag-psd/idb so the app never finished booting. Fix: kill the vite on port 5173, rm -rf node_modules/.vite, restart (first page load re-bundles deps, ~20s). After the fix the browser suite is reliable; verifiers hitting boot timeouts should apply the same cache clear.",
      "Full gate run (this session, after the cache fix): npx tsc --noEmit exit 0; npm run test:engines PASS; npm run test:unit 316/316; npm run test:chrome 10/10; npm run test:slices 13/13 (one environmental server/port flake on a first run, clean on immediate re-run; tests/slices.mjs is byte-identical to main so the delta cannot affect it); npm run build exit 0; compound node test 5/5 standalone."
    ]
  },
  "verifier": {
    "status": "PENDING",
    "notes": "Independent verification required after implementation. Reproduction recipe for the before-state: boot the app, `window.viroAnchor.applyDetailed` two overlapping rects, `P.doc.activeLayerIds = [innerId, outerId]`, `P.subtractSelected()`, then `await import('/src/export/pdf.ts')` and call exportPagePdf with the same arguments app.exportPdf uses. Assert report.vectorPaths and inflate the FlateDecode content streams — path operators m/c/h are absent for compound layers and present for a single-contour control.",
    "proof": []
  },
  "critic": {
    "status": "PENDING",
    "notes": "Exported PDF is user-visible output; critic must judge visual fidelity against the canvas rather than UI chrome.",
    "proof": []
  },
  "delta": {
    "before": "Compound boolean geometry is authoritative in the document/compositor, but src/export/pdf.ts emitVector reads only layer.nodes/layer.closed. Since boolean results carry nodes: [], every compound vector is silently dropped from the vector PDF and PdfExportReport reports vectorPaths 0 with no raster fallback.",
    "after": "emitVector reads layer.contours with the same precedence rule as the compositor, emits every drawable contour (moveTo/cubicTo/closePath per contour), and selects the even-odd PDF fill operator ('f*'/'B*') exactly when the compositor would set FillType.EvenOdd (multi-contour with >1 drawable). Legacy single-contour vectors are unaffected (verified byte-identical operator counts). PdfExportReport counts a compound layer as one vector path and records no rasterFallback for it.",
    "proof": [
      "tests/pdf-compound.test.mjs — 5/5, run both standalone and combined with the 12 baseline unit files (321/321) — see builder.proof for the exact operator-count assertions per case",
      "tests/pdf-compound.spec.ts (committed cfc9b77) — 5/5 under npm run test:chrome (10/10 suite total); full gate run this session: tsc 0, engines PASS, unit 316/316, chrome 10/10, slices 13/13, build 0"
    ],
    "regression_protection": [
      "legacy single-contour vector: m=1 c=4 h=1 f=1 (unchanged from pre-fix baseline measured by the audit)"
    ],
    "new_debt": "none accepted — no pixel-level (rasterized) proof of the hole in this unit-test pass; verifier's reproduction recipe in this manifest covers that with a real render if required"
  }
}
