name: Orchestrator Integrity

on:
  pull_request:
    paths:
      - ".viro/**"
      - ".claude/agents/**"
      - ".cursor/**"
      - "AGENTS.md"
      - "CLAUDE.md"
      - "docs/agents/**"
      - "scripts/viro-orchestrator.mjs"
      - "scripts/viro-release.mjs"
      - "scripts/lib/orchestrator-core.mjs"
      - "scripts/lib/release-gate.mjs"
      - "tests/orchestrator.test.mjs"
      - "tests/release-gate.test.mjs"
      - ".github/workflows/orchestrator-integrity.yml"
      - ".github/workflows/auto-merge-approved.yml"
  push:
    branches: [main]
    paths:
      - ".viro/**"
      - ".claude/agents/**"
      - ".cursor/**"
      - "AGENTS.md"
      - "CLAUDE.md"
      - "docs/agents/**"
      - "scripts/viro-orchestrator.mjs"
      - "scripts/viro-release.mjs"
      - "scripts/lib/orchestrator-core.mjs"
      - "scripts/lib/release-gate.mjs"
      - "tests/orchestrator.test.mjs"
      - "tests/release-gate.test.mjs"
      - ".github/workflows/auto-merge-approved.yml"

permissions:
  contents: read

jobs:
  state-machine:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Test scheduling, leases, handoffs and release gates
        run: node --test tests/orchestrator.test.mjs tests/release-gate.test.mjs
      - name: Prove orchestrator CLI loads without credentials
        run: node scripts/viro-orchestrator.mjs help
      - name: Prove release CLI loads without credentials
        run: node scripts/viro-release.mjs help
