# Cloudflare Pages redirects for the VIRO Press SPA.
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# SPA history fallback: serve index.html (HTTP 200, not a redirect) for any path
# that is not a real static file, so client-side routes resolve.
#
# This does NOT shadow real assets: Cloudflare Pages serves an existing static
# file whenever the request path matches one (e.g. /assets/*, /wasm/*, /ml/*,
# /fonts/*, /favicon.ico). The fallback below only runs for paths with no
# matching file on disk.
/*    /index.html    200
